<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PoseDetectionKt" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
    </style>

    <style name="Theme.PoseDetectionKt" parent="Base.Theme.PoseDetectionKt" />

    <!--<style name="NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>-->

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="TextAppearance.AppCompat" parent="Theme.Material3.Light">
        <item name="android:textColor">@color/my_light_primary</item>
    </style>
</resources>